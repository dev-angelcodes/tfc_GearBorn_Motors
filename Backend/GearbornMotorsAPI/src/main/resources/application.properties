# --- Nombre de la app ---
spring.application.name=GearbornMotors

# --- Configuraci�n de la base de datos ---
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/tfc_bd_gearborn?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# --- Configuraci�n de JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- Puerto del servidor para las peticiones HTTP ---
server.port=8080
# --- Context Path ---
server.servlet.context-path=/gearBorn/api
